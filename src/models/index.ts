export type dataAppbroaches = {
  ellipse: JSX.Element;
  title: string;
  content: string;
}[];

export type dataHire = { title: string; content: string }[];

export type dataProcess = {
  title: string;
  option: string[];
  imageUrl: { src: string; alt: string };
}[];

export type dataDevServ = { title: string; option: string[] }[];

export type dataLogo = { logo: string; name: string }[];

export type dataString = string[];
